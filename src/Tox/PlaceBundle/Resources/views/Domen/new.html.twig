{% extends 'ToxPlaceBundle:Domen:domen.html.twig' %}

{% block domen %}
<h1>Domen creation</h1>

<form action="{{ path('domen_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('domen') }}">
            Back to the list
        </a>
    </li>
</ul>
        {% endblock %}