{% extends 'ToxPlaceBundle:Host:host.html.twig' %}

{% block host %}
<h1>Register edit</h1>
<ul class="record_actions">
    <li>
        <a href="{{ path('host') }}">←</a>
    </li>
    <li>
        <form action="{{ path('host_delete', { 'id': host.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>

{% include 'ToxPlaceBundle:Host:form.html.twig' with {'form': form,'host':host } %}


        {% endblock %}