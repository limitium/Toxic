{% extends 'ToxParserBundle:Source:source.html.twig' %}

{% block source %}
<h1>Source edit</h1>
<ul class="record_actions">
    <li>
        <a href="{{ path('source') }}">←</a>
    </li>
    <li>
        <form action="{{ path('source_delete', { 'id': source.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>

{% include 'ToxParserBundle:Source:form.html.twig' with {'form': form,'source':source } %}


        {% endblock %}